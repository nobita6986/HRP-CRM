// copy.mjs -- placeholder build step.
// This package ships pre-built ESM modules under dist/.
// Future revisions may emit them by transpiling TypeScript; for
// now dist/ is committed verbatim and this script is a no-op.
console.log('n8n-nodes-hrp-signer: dist/ already up-to-date, no copy needed');