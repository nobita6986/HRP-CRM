# G0/0.0–0.2 — Handoff bundle

**T1 (Coder)**: 2026-09-13. Repo `https://github.com/nobita6986/HRP-CRM.git`,
local branch `main`, no commits yet. Workspace `D:\CodeApp\Hrp-Crm`.

## Trạng thái thực tế

- Repo chỉ có sáu tài liệu `docs/Importal/*`. Không có checkout HRP core,
  schema Prisma, `AGENTS.md`, `AI_CODING_GUARDRAILS.md`, auth specification
  hoặc endpoint đã xác nhận → contracts không đối chiếu được với core.
- Git local đã init trên branch `main`, chưa có commit/HEAD.
- `packages/contracts/` đã được khởi tạo trước (skeleton `package.json`,
  `tsconfig.json`, `node_modules/`, `dist/` cũ). Đã giữ nguyên, đã rebuild.
- Workspace không có app/UI/backend thật. Không cài thêm tooling ngoài
  Node 24.19 / npm 11.17 / git 2.55 đã có sẵn.

## Môi trường đã cài

| Thành phần | Phiên bản | Lệnh kiểm chứng |
|---|---|---|
| Node.js | v24.19.0 | `node --version` |
| npm | 11.17.0 | `npm --version` |
| git | 2.55.0 | `git --version` |
| TypeScript | 5.7.3 (dep của package) | `npm ls typescript` |
| Zod | 3.24.2 (dep của package) | `npm ls zod` |

Lệnh kiểm chứng (chạy trong `packages/contracts/`):

```bash
npm install            # đã chạy trước; idempotent
npm run typecheck      # tsc --noEmit (strict, noUncheckedIndexedAccess,
                       # exactOptionalPropertyTypes)
npm test               # npm run build && node --test tests/*.test.mjs
```

## Changed files

```
packages/contracts/src/enums.ts            (giữ từ session trước)
packages/contracts/src/primitives.ts       (giữ từ session trước)
packages/contracts/src/errors.ts           (giữ từ session trước)
packages/contracts/src/envelopes.ts        (giữ từ session trước)
packages/contracts/src/index.ts            (giữ từ session trước)
packages/contracts/tests/enums.test.mjs    (giữ từ session trước)
packages/contracts/tests/errors.test.mjs   (giữ từ session trước)
packages/contracts/tests/envelopes.test.mjs(giữ từ session trước)
packages/contracts/tests/contracts.synthetic.mjs  (rename từ
                                                  contracts.test.mjs của
                                                  agent nội bộ trước)
packages/contracts/tests/_synthetic-pending.md    (mới — giải thích lý do)
docs/contracts/inventory.md                (giữ + bổ sung mục cuối)
docs/contracts/decision-register.md        (giữ từ session trước)
```

## AC từng mục — PASS/FAIL/BLOCKED + evidence

### G0/0.0 — Inventory và ownership

| AC | Trạng thái | Evidence |
|---|---|---|
| Mỗi command/query/event có owner, consumer, trạng thái, nguồn | PASS | `docs/contracts/inventory.md` §Commands/§Queries/§Events có owner→consumer + nguồn cho ~25 mục |
| Ghi rõ open-status / transitions / review pre-post-apply / managed-mode / KPI attribution là cần domain review | PASS | `docs/contracts/decision-register.md` D-001 → D-021 và §"Open questions cho Owner / HRP domain" với 12 câu hỏi Q-01..Q-12 |
| Phân biệt Integration DB được phép với HRP core không được truy cập | PASS | `inventory.md` §"Storage / gates" + decision-register D-014, D-019 |

### G0/0.1 — Enums và constants

| AC | Trạng thái | Evidence |
|---|---|---|
| PlacementCase stages đúng 8 giá trị | PASS | `enums.ts` `PLACEMENT_CASE_STAGES`; `tests/enums.test.mjs` "placement stages đúng 8 giá trị đã chốt" |
| Status đóng duy nhất CLOSED; closeReason đúng 9 giá trị | PASS | `CLOSED_CASE_STATUS`, `CASE_CLOSE_REASONS`; tests "case status chỉ ACTIVE/CLOSED", "close reason đúng 9 giá trị" |
| Availability đúng 5 giá trị | PASS | `AVAILABILITIES`; test "availability đúng 5 giá trị" |
| CurrentRelationship đúng 5 giá trị; chỉ trong read result | PASS | `CURRENT_RELATIONSHIPS` + `CURRENT_RELATIONSHIP_READONLY`; test "current relationship đúng 5 giá trị, đánh dấu read-only" |
| Không CLOSED_SUCCESS / closeReason làm stage / status không nghe máy | PASS | `FORBIDDEN_CLOSED_VARIANTS`; test "closed_variants cấm luôn bị reject" |
| NextAction OPEN/DONE/CANCELLED + matching outcomes khai báo riêng | PASS | `NEXT_ACTION_STATUSES`, `MATCHING_OUTCOMES`; tests riêng |

### G0/0.2 — Envelopes + error taxonomy

| AC | Trạng thái | Evidence |
|---|---|---|
| ACCEPTED có durable op reference, không có data | PASS | `AcceptedResponseSchema`; test "ACCEPTED response có operation reference, không có data" |
| APPLIED có data, errors rỗng | PASS | `applyApplied()` + `AppliedResponseBaseSchema`; tests "APPLIED response yêu cầu data, errors rỗng" + "APPLIED base schema chấp nhận data unknown" |
| FAILED ≥1 error, không data | PASS | `FailedResponseSchema`; test "FAILED response không được có data hữu dụng" |
| Taxonomy tối thiểu 5 codes (đã mở rộng 10); retryability rules | PASS | `ErrorCodeSchema` 10 codes; `ERROR_RETRYABLE_DEFAULT`; tests "error code đủ 10 loại", "retry defaults …" |
| Không raw stack/SQL/provider body/secret; field paths không echo PII | PASS | `StructuredErrorSchema` chỉ field an toàn + `messageKey`; tests "makeError cấm chứa raw stack", "field path phải là JSON pointer" |
| Validate format/size/version của IDs/timestamps; không ép UUID | PASS | `CanonicalIdSchema`/`CommandIdSchema`/`IdempotencyKeySchema` dùng regex opaque; tests "idempotency key vượt giới hạn độ dài bị reject", "unknown schema version bị reject" |
| Same key + same/different payload semantics; scope bao gồm organization/command | PASS | `idempotencyDigest` + `canonicalize`; tests "idempotency: same payload digest → trùng", "different payload digest → khác", "canonicalize sort key ổn định" |
| CorrelationId không thay idempotency key | PASS | test "correlation id và idempotency key tồn tại độc lập" |
| Malformed actor/source, unknown version, inconsistent response status bị reject | PASS | tests "malformed actor (USER có delegatedBy) bị reject", "unknown schema version bị reject", "FAILED response không được có data hữu dụng" |

## Tóm tắt test

```
$ npm test
…
ℹ tests 32
ℹ pass 32
ℹ fail 0
ℹ duration_ms 307.9346
```

## Limitations / Missing inputs

1. **Không có checkout HRP core.** Không đọc được schema Prisma, domain
   services, `AGENTS.md`, `AI_CODING_GUARDRAILS.md`, auth/IdP thật, hoặc
   endpoint thực. Contracts chỉ đối chiếu Master V2.6 / connector v1.0 /
   Backlog Gate0. Mọi claim "typecheck pass" KHÔNG chứng minh runtime HRP
   đã xác thực.
2. **Connector trên đĩa là v1.0 (Master V2.5)**, không v1.1 mà
   guide/backlogs dẫn chiếu. Master V2.6 vẫn là baseline.
3. **`contracts.synthetic.mjs`** (rename từ `contracts.test.mjs`) chứa test
   do agent nội bộ đặt; Coder KHÔNG đại tu contracts theo file này vì
   một số ràng buộc mâu thuẫn baseline (`SCHEMA_VERSION='1'` bị reject,
   actor `DELEGATED_USER` không có trong Master). File đã đổi extension
   để không chạy trong default runner; nội dung giữ nguyên để đối chiếu.
   Trạng thái: BLOCKED-OWNER.
4. **Open-status / transitions / managed-mode / OTHER-explanation / actor
   cho automated interactions / DNC fencing / signature protocol / event
   retention** đã liệt kê trong `decision-register.md` Q-01..Q-12 — cần
   Owner/Chủ nhân/HRP-side chốt trước khi G0/0.3–0.7.
5. **Mock** KHÔNG chứng minh concurrency Prisma HRP / provider authenticity
   / production readiness. Theo Backlog §A.7.

## Bundle ổn định

Repo chưa có commit. Hash manifest của working tree tại HEAD hiện tại
(nội dung các file đã tạo/giữ, không tracked):

```
$ git ls-files --others --exclude-standard
.gitignore
docs/contracts/inventory.md
docs/contracts/decision-register.md
packages/contracts/package.json
packages/contracts/package-lock.json
packages/contracts/README.md
packages/contracts/tsconfig.json
packages/contracts/src/index.ts
packages/contracts/src/enums.ts
packages/contracts/src/primitives.ts
packages/contracts/src/errors.ts
packages/contracts/src/envelopes.ts
packages/contracts/tests/enums.test.mjs
packages/contracts/tests/errors.test.mjs
packages/contracts/tests/envelopes.test.mjs
packages/contracts/tests/contracts.synthetic.mjs
packages/contracts/tests/_synthetic-pending.md
```

`dist/` (build output) và `node_modules/` đã có trong `.gitignore`.

Coder đề xuất commit khi Owner/Chủ nhân xác nhận Gate 0. Hiện tại không
tự commit để chờ Owner review bundle và confirm.

## Risk classification (theo Execution-Guide §5.3)

- **Shared contract quan trọng (actor/source/envelope)** — thuộc diện
  audit. Coder đã self-check bằng typecheck strict + 32 fixtures runtime;
  KHÔNG tự coi là independent audit. Owner/Chủ nhân quyết gom audit
  cùng bundle Gate 0 (theo prompt).
- Không thuộc diện HRP core writes / auth / PII direct / data reliability
  side effects / AI side effects — chỉ contracts deterministic.
- Không chạm backend, Prisma, migration, Route Handler, provider thật,
  deploy, gửi tin khách. Cài môi trường không cấp quyền các bước này.
