{
  "name": "@hrp-engagement/core-1.10-media",
  "version": "1.0.0-core1.10",
  "private": true,
  "description": "CORE/1.10 — Media/secret/storage ports mock + policy harness. Synthetic evidence lifecycle (READY/QUARANTINED/REJECTED/REVOKED), fake secret provider returning refs only, SSRF/cross-org/public-evidence policy harness. No real antivirus, no VN production storage. ADR-MEDIA-01-VN.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "policy-test": "node --test tests/policy-harness.test.mjs",
    "generate-manifest": "node scripts/generate-manifest.mjs",
    "verify-manifest": "node scripts/generate-manifest.mjs --verify",
    "check-manifest": "node scripts/generate-manifest.mjs --check"
  },
  "dependencies": {
    "@hrp-engagement/contracts": "file:../../packages/contracts",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "22.10.2",
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
